# Construction Mini ERP - Backend

Node.js + Express backend for the Construction Mini ERP prototype.

Setup

1. copy `.env.example` to `.env` and set DB credentials
2. run `npm install`
3. create the database using `sql/schema.sql`
4. run `npm run dev`

API endpoints documented in root README.
